# golang-test-cli
test https://github.com/spf13/cobra

```text
A longer description that spans multiple lines and likely contains
examples and usage of using your application. For example:

Cobra is a CLI library for Go that empowers applications.
This application is a tool to generate the needed files
to quickly create a Cobra application.

Usage:
  golang-test-cli [command]

Examples:
golang-test-cli version

Available Commands:
  cwd         Change current directory
  pwd         Prints process working directory to stdout
  version     Print the version number of golang-test-cli

Flags:
      --config string   config file (default is $HOME/.golang-test-cli.yaml)
  -v, --verbose         verbose output

Use "golang-test-cli [command] --help" for more information about a command.
```
